const endpoint = 'https://cmuniversity.azurewebsites.net';
const apis = {
    account: endpoint + "/api/account/",
    user: endpoint + "/api/user/",
    manager: endpoint + "/api/manager/",
    admin: endpoint + "/api/admin/",
    coordinator: endpoint + "/api/coordinator/",
    faculty: endpoint + "/api/Faculties/",
    role: endpoint + "/api/Roles/",
    event: endpoint + "/api/events/",
    article: endpoint + "/api/Articles/",
    normal: endpoint + "/api/",
    comment: endpoint + "/api/comment/",
    like: endpoint + "/api/like/",
    dislike: endpoint + "/api/dislike/",
    notification: endpoint + "/api/notification/",
};

export default apis;
